#!/usr/bin/env sh
# Installed via `make install-hooks` (sets core.hooksPath to this directory).
set -eu

fmt_out="$(gofmt -l .)"
if [ -n "$fmt_out" ]; then
  echo "gofmt found issues in:"
  echo "$fmt_out"
  echo "Run: gofmt -w ."
  exit 1
fi

go vet ./...
